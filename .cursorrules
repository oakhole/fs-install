# Cursor / Agent 行为规则

## 语言与输出
- 始终使用简体中文进行回答、解释、分析和建议。
- 代码、命令、变量名、API、库名保持英文原文，不要翻译。
- 回答保持简洁、直接，避免无意义的寒暄和铺垫。

## 行为边界
- 所有操作严格限制在当前项目目录内，不得修改、删除或影响项目目录之外的文件与资源。
- 优先遵循仓库现有结构、命名、字段风格和分层方式，不擅自引入与现有模式冲突的新约定。
- 遇到不明确的接口、契约或能力时，先查仓内文档、代码和已有实现；仍不确定时再明确说明不确定点并请求确认。

## 禁止幻象
- 禁止编造不存在的库函数、工具能力、配置项、命令或 API。
- 不要基于猜测描述第三方系统行为；涉及 Kamailio、RTPEngine 或其他外部系统时，先依据官方文档或仓内契约再下结论。
- 无法确认时明确说明“未知”或“需确认”，不要伪造确定性结论。

## 代码与注释原则
- 修改代码时遵循仓库既有语言规范：Go、Java、TypeScript/React、SQL 等保持项目当前风格一致。
- 新增或修改复杂逻辑时，补充简体中文注释，说明功能目的、输入输出、异常处理，以及关键业务逻辑“为什么这样做”。
- 跨边界字段命名优先保持 `snake_case`，并尽量复用已有 schema / contract 词汇。

## 性能与可追踪性
- 涉及数据库操作时，考虑连接池、高并发下的索引效率与查询成本。
- 涉及 Redis 或其他缓存时，设置合理过期时间，避免内存无限增长。
- 涉及日志与链路追踪时，保留 `trace_id` 或等效全局标识，确保问题可追踪。
