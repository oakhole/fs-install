# [AGENTS.md](http://AGENTS.md)

## 最高优先级规则

- 始终使用简体中文回答、解释、分析和建议。
- 代码、命令、变量名、API、库名保持英文原文，不要翻译。
- 回答简洁、直接，避免无意义寒暄。
- 所有操作必须限制在当前项目目录内；禁止修改、删除或影响项目目录之外的任何文件或资源。
- 不确定的事实、接口、能力、配置或第三方 API，先查仓内代码、文档、契约和官方资料；仍不确定时明确说明并请求确认。
- 禁止编造不存在的库函数、命令、配置项、工具能力或 API。

## 工作区与代码同步

- 开始任务前必须先检查工作区状态：运行 `git status`。
- 若存在未提交变更，必须先让用户选择：
  1. 自动提交当前变更，推荐此项。
  2. 丢弃当前变更。
  3. 忽略当前变更并继续。
- 只有在工作区干净后，才执行 `git pull` 拉取最新代码。
- 未经用户明确要求，不得执行破坏性命令，如 `git reset --hard`、批量删除、强制覆盖。

## 执行限制

- 针对同一编译或构建错误，最多连续尝试修复 3 次。
- 第 3 次仍失败时，立即停止，并向用户提供：
  - 完整错误信息
  - 已尝试方案摘要
  - 可能根因与下一步建议

## 代码与命名规则

- 优先遵循仓库既有结构、分层、命名和字段风格，不擅自引入冲突的新约定。
- 前端包管理默认使用 `pnpm`，除非用户明确要求其他工具。
- Go：使用 `gofmt`，tab 缩进，业务代码优先放在 `internal/`。
- Java：4 空格缩进；类名 `PascalCase`；方法和字段 `camelCase`；按 `controller`、`service`、`service/impl`、`model`、`mapper` 分层。
- TypeScript/React：2 空格缩进；组件使用 `PascalCase`；路由页放在 `src/pages/**`。
- SQL：迁移文件使用 `V<number>__description.sql`。
- OpenAPI、Kafka、JSON 等跨边界字段统一优先使用 `snake_case`，并复用已有 schema / contract 词汇。

## 注释与可维护性

- 每个新增或修改的重要函数，应补充简体中文注释，说明：
  - 功能
  - 输入参数逻辑
  - 预期输出
  - 异常处理
- 关键业务逻辑必须说明“为什么这样做”，不要只描述“做了什么”。

## 性能与可追踪性

- 涉及 MySQL 或 PostgreSQL 时，考虑连接池、高并发下的索引效率与查询成本。
- 涉及 Redis 时，设置合理过期时间，避免内存无限增长。
- 日志必须包含 `trace_id` 或等效全局标识，保证链路可追踪。

## 测试底线

- 新增行为或修改行为时，补充最小可验证测试。
- Go：测试文件命名为 `*_test.go`，放在被测包旁边。
- Java：使用 JUnit 5，通过 `gradle test` 或 `maven test` 运行。
- 前端：若仓库未接入测试框架，至少执行 `pnpm lint`，并手动验证关键页面。

## 提交与 Pull Request

- 提交信息必须使用简体中文，准确概括本次变更，禁止使用 `update`、`fix` 等无意义标题。
- 每次提交必须关联明确的 issue 或 ADR。
- Pull Request 必须包含：变更摘要、影响目录、关联 issue 或 ADR。
- 涉及前端页面时附截图；涉及 schema、contract、配置发布时明确说明影响范围。

